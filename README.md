# Lumina Companion Source

To build as an APK:
1. Use Capacitor: `npm install @capacitor/core @capacitor/cli`
2. Initialize: `npx cap init`
3. Add platform: `npx cap add android`
4. Copy these files to the `www` or `dist` folder.
5. Build: `npx cap open android`